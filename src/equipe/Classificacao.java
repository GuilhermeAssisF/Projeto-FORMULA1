// Declaração do pacote onde a interface está localizada
package equipe;

// Definição da interface Classificacao
public interface Classificacao {
    // Método abstrato para obter a pontuação com base na posição
    int obterPontuacao(int posicao);
}